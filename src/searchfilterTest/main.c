#include "../common/define.h"
#include "../searchfilter/searchfilter.h"

int main() {

	struct DocumentIndexFormat DocumentIndex;

	sprintf(DocumentIndex.Url, "http://bo-derek-nude-jennifer-lopez-topless.sex-city.info/index.html");

	if(filterDocumentIndex(&DocumentIndex)) {

	}
}
